import { Button } from '@/components/ui/button';

export function UserUpload() {
  return (
    <Button className="w-full rounded-xl">
      Request active joined chats in a *.txt file
    </Button>
  );
}
